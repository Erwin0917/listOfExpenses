import styled from '@emotion/styled-base';
import Button from '../../components/UI/Button';
import { Color } from '../const/Color';

export const ButtonRemoveStyles = styled(Button)`
	background-color: ${Color.ACCENT_NEGATIVE};
`;
