import styled from '@emotion/styled';
import { Color } from '../const/Color';

export const ButtonStyles = styled.button`
	border: none;
	background-color: ${Color.ACCENT_FIRST};
	color: white;
	font-size: 1.8rem;
	padding: 5px 15px;
	border-radius: 4px;
`;
