import styled from '@emotion/styled';
import { Color } from './const/Color';

export const ErrorMessageStyles = styled.p`
	color: ${Color.ACCENT_NEGATIVE};
	width: 100%;
	text-align: right;
	padding: 0 10px;
	font-size: 1rem;
`;
