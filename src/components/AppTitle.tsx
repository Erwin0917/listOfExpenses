import React from 'react';
import { AppTitleStyles } from '../styles/AppTitleStyles';

const AppTitle = () => {
	return <AppTitleStyles>List of expenses</AppTitleStyles>;
};

export default AppTitle;
